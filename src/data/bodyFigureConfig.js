/** On-screen frame — identical for male and female. */
export const BODY_FIGURE_VIEW = {
  width: 240,
  height: 420,
};

/** Source PNG dimensions (all four images share this size). Hit zones use this, not per-image measurement. */
export const BODY_FIGURE_NATURAL = {
  width: 979,
  height: 2400,
};

export const BODY_SIDE_KEYS = {
  LEFT: 'left',
  RIGHT: 'right',
  CENTER: 'center',
};

/** Set to true to show the Calibrate positions controls on Muscle Map. */
export const MUSCLE_MAP_CALIBRATION_ENABLED = false;
